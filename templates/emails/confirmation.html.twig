{% extends 'emails/base_email.html.twig' %}

{% block content %}
<h1 style="color: #0d6efd;">Confirmation de compte</h1>

<p>Bonjour {{ user.pseudo }},</p>

<p>Merci de confirmer votre adresse email en cliquant sur le lien suivant :</p>

<div style="text-align: center; margin: 30px 0;">
    <a href="{{ absolute_url(path('app_auth_confirm_email', { token: token })) }}" 
       style="background-color: #0d6efd; color: white; padding: 12px 25px; 
              text-decoration: none; border-radius: 5px;">
       Confirmer mon compte
    </a>
</div>

<p>Ce lien expirera le {{ expiration_date|format_datetime('long', 'short')|default(expiration_date|date('d/m/Y H:i')) }}.</p>

<p style="margin-top: 30px; color: #666;">
    Si vous n'êtes pas à l'origine de cette demande, veuillez ignorer cet email.
</p>
{% endblock %}
