{% extends 'emails/base_email.html.twig' %}

{% block content %}
	<h1>Réinitialisation de mot de passe</h1>

	<p>Bonjour
		{{ user.pseudo }},</p>

	<p>Pour définir un nouveau mot de passe, cliquez sur le bouton ci-dessous :</p>

	<div style="text-align: center; margin: 30px 0;">
		<a href="{{ url('app_auth_reset_password', {token: token}) }}" class="btn">
			Réinitialiser mon mot de passe
		</a>
	</div>

	<p class="text-muted">Ce lien expirera le
		{{ expiration_date|format_datetime('medium', 'short') }}.</p>
	<p class="text-muted">Si vous n'avez pas demandé cette réinitialisation, ignorez cet email.</p>
{% endblock %}
