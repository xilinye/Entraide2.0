parameters:
  blog_images_directory: "%kernel.project_dir%/public/uploads/test/blog"

services:
  _defaults:
    autowire: true
    autoconfigure: true

  App\Tests\:
    resource: "../tests/*"
    exclude: "../tests/{Entity,Fixtures,KernelTestCase.php}"
