<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>
			{% block title %}Entr'Aide 2.0
			{% endblock %}
		</title>
		<style>:root
		{
			--primary-color: #4CAF50;
			--primary-dark: #45a049;
			--secondary-color: #607D8B;
			--background-light: #f8f9fa;
			--text-color: #333333;
			--border-color: #e0e0e0;
		}

		body {
			font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
			line-height: 1.6;
			color: var(--text-color);
			margin: 0;
			background-color: #ffffff;
		}
		.container {
			max-width: 600px;
			margin: 0 auto;
			padding: 20px;
		}
		h1,
		h2,
		h3 {
			color: var(--primary-color);
			margin-top: 0;
		}
		.footer {
			margin-top: 40px;
			padding-top: 20px;
			border-top: 2px solid var(--border-color);
			color: var(--secondary-color);
			font-size: 0.9em;
			text-align: center;
		}
		.btn {
			display: inline-block;
			background-color: var(--primary-color);
			color: white !important;
			padding: 12px 25px;
			text-decoration: none;
			border-radius: 5px;
			font-weight: 500;
			transition: background-color 0.3s ease;
		}
		.btn:hover {
			background-color: var(--primary-dark);
		}
		.message-box {
			background-color: var(--background-light);
			padding: 15px;
			border-radius: 8px;
			margin: 20px 0;
			border-left: 4px solid var(--primary-color);
		}
		.text-muted {
			color: var(--secondary-color);
			font-size: 0.9em;
		}
	</style>
</head>
<body>
	<div class="container"> {% block content %}{% endblock %}

		<div class="footer">
			<p>Cet email vous a été envoyé automatiquement par
				<a href="{{ absolute_url(path('app_page_home')) }}" style="color: var(--primary-color); text-decoration: none; font-weight: 500;">
					Entr'Aide 2.0
				</a>
			</p>
		</div>
	</div>
</body></html>
